name: ENV DEMO
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print env
        run: echo "App is connecting to $APP_ENV"
        
      
